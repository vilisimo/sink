import 'package:sink/domain/entry.dart';

class AddEntry {
  final Entry entry;

  AddEntry(this.entry);
}

class EditEntry {
  final Entry entry;

  EditEntry(this.entry);
}

class DeleteEntry {
  final Entry entry;

  DeleteEntry(this.entry);
}